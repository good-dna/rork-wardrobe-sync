# Outfit Plan Implementation\n\nThis implementation provides the exact JS script you requested for saving outfit plans to the database.\n\n## The Implemented Script\n\nThe core functionality implements this exact pattern:\n\n```javascript\n// selected is a JS Date for the day the user tapped\nconst ymd = selected.toLocaleDateString('en-CA'); // \"2025-08-31\" (safe ISO-like)\nawait supabase.from('plans').insert({\n  user_id: user.id,\n  date_ymd: ymd,\n  outfit_id: outfitId,\n  notes\n});\n```\n\n## Implementation Files\n\n### 1. Backend tRPC Routes\n- **File**: `backend/trpc/routes/plans/route.ts`\n- **Purpose**: Handles database operations for outfit plans\n- **Routes**: add, getByDate, getByDateRange, update, delete, get, getAll\n\n### 2. Service Layer\n- **File**: `services/planService.ts`\n- **Purpose**: Provides utility functions that implement the exact script\n- **Key Functions**:\n  - `saveOutfitPlan()` - Implements the exact script pattern\n  - `getPlansForDate()` - Get plans for a specific date\n  - `getPlansForDateRange()` - Get plans for calendar views\n\n### 3. React Hook\n- **File**: `hooks/usePlans.ts`\n- **Purpose**: React Query integration for outfit plans\n- **Features**: Caching, optimistic updates, error handling\n\n### 4. Example Component\n- **File**: `components/ExamplePlanComponent.tsx`\n- **Purpose**: Demonstrates how to use the implemented script\n\n## Usage Examples\n\n### Method 1: Using the Service Function\n\n```typescript\nimport { saveOutfitPlan } from '@/services/planService';\n\nconst handleSave = async () => {\n  const selected = new Date(); // JS Date for the day the user tapped\n  const outfitId = 'outfit_123';\n  const notes = 'Casual Friday outfit';\n  const user = { id: 'user_456' };\n  \n  await saveOutfitPlan(\n    selected,\n    outfitId, \n    notes,\n    user,\n    {\n      name: 'Friday Casual',\n      category: 'casual',\n      items: ['shirt_1', 'pants_2', 'shoes_3'],\n      reminderEnabled: true\n    }\n  );\n};\n```\n\n### Method 2: Using the React Hook\n\n```typescript\nimport { usePlans } from '@/hooks/usePlans';\n\nconst MyComponent = () => {\n  const { savePlan } = usePlans();\n  \n  const handleSave = async () => {\n    const selected = new Date(); // JS Date for the day the user tapped\n    const outfitId = 'outfit_123';\n    const notes = 'Casual Friday outfit';\n    const user = { id: 'user_456' };\n    \n    await savePlan(\n      selected,\n      outfitId,\n      notes, \n      user,\n      {\n        name: 'Friday Casual',\n        category: 'casual',\n        items: ['shirt_1', 'pants_2', 'shoes_3'],\n        reminderEnabled: true\n      }\n    );\n  };\n  \n  return (\n    <TouchableOpacity onPress={handleSave}>\n      <Text>Save Plan</Text>\n    </TouchableOpacity>\n  );\n};\n```\n\n### Method 3: Direct tRPC Client Usage\n\n```typescript\nimport { trpcClient } from '@/lib/trpc';\n\nconst handleSave = async () => {\n  const selected = new Date(); // JS Date for the day the user tapped\n  const ymd = selected.toLocaleDateString('en-CA'); // \"2025-08-31\" (safe ISO-like)\n  \n  await trpcClient.plans.add.mutate({\n    date_ymd: ymd,\n    outfit_id: 'outfit_123',\n    name: 'Friday Casual',\n    category: 'casual',\n    items: ['shirt_1', 'pants_2', 'shoes_3'],\n    notes: 'Casual Friday outfit',\n    reminder_enabled: true,\n  });\n};\n```\n\n## Data Structure\n\nThe plans are stored with this structure:\n\n```typescript\ninterface Plan {\n  id: string;\n  user_id: string;\n  date_ymd: string; // \"2025-08-31\" format\n  outfit_id?: string;\n  name: string;\n  category: 'casual' | 'formal' | 'work' | 'athletic' | 'evening' | 'special';\n  items: string[];\n  notes?: string;\n  reminder_enabled?: boolean;\n  created_at: string;\n  updated_at: string;\n}\n```\n\n## Key Features\n\n1. **Exact Script Implementation**: The core functionality matches your requested script exactly\n2. **Date Formatting**: Uses `toLocaleDateString('en-CA')` for ISO-like date format\n3. **Type Safety**: Full TypeScript support with proper types\n4. **React Query Integration**: Automatic caching and state management\n5. **Error Handling**: Comprehensive error handling and logging\n6. **Flexible API**: Multiple ways to use the functionality\n\n## Testing\n\nYou can test the implementation using the `ExamplePlanComponent`:\n\n```typescript\nimport { ExamplePlanComponent } from '@/components/ExamplePlanComponent';\n\nconst TestScreen = () => {\n  return (\n    <ExamplePlanComponent\n      selectedDate={new Date()}\n      outfitId=\"test_outfit_123\"\n      onPlanSaved={() => console.log('Plan saved!')}\n    />\n  );\n};\n```\n\nThe implementation is now ready to use and follows the exact pattern you specified!"